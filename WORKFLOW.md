# WORKFLOW

本文档说明主动营销点将期间每天如何使用 `ai-work-tools`。

## 核心原则

- 先录数据，再做 AI 研判。
- 先 AI 研判，再生成日报。
- 日报生成后必须允许人工编辑。
- AI 研判是辅助判断，不替代现场判断。
- 线索先进池，确认有效再进 CRM。
- CRM 是正式客户主台账，用于持续跟进、留档复盘、漏斗统计和转化分析。

## 1. 早上：明确今日方向

1. 打开 `city-analysis.html`。
2. 查看昨日/今日 AI 研判。
3. 读取今日系统数据。
4. 明确今日重点动作。
5. 确定今日客户跟进、线索清洗、素材测试方向。
6. 查看活动政策/机制资料库，确认当天活动口径。

## 2. 白天：录入与跟进

1. 做销售客户池复盘。
2. 在 `customer-review.html` 按销售记录客户池。
3. 逐个销售确认 A/B/C、已量房、已报价、待跟进、沉睡客户和新线索。
4. 在 `crm.html` 录入或更新需要持续跟进、留档复盘的客户明细。
5. 抖音线索先进 `douyin-leads.html`。
6. 抖音线索清洗为有效后再转 CRM。
7. 每次跟进客户后更新跟进记录。
8. 填写下次跟进时间。
9. 维护今日待跟进和逾期未跟进。
10. 对明确无效、已死单或已流失客户，及时做死单标记和原因记录。

## 3. 晚上：复盘与输出

1. 在 `crm.html` 更新当天所有跟进动作。
2. 在 `douyin-leads.html` 清洗当天抖音线索。
3. 在 `city-analysis.html` 读取今日系统数据。
4. 保存今日 AI 研判。
5. 在 `report.html` 读取客户池、CRM、抖音线索、AI 研判。
6. 生成并人工编辑日报。
7. 必要时生成周报或阶段汇报。
8. 在 `material-lab.html` 生成明日信息流素材矩阵或 AI 调令。
9. 如果有投流测试，更新 `ad-test.html` 的投流记录和复盘判断。

## 4. 数据流转关系

### 抖音线索流转

1. 抖音信息流、私信、评论、直播、主页留资进入 `douyin-leads.html`。
2. 判断有效/无效。
3. 分配销售。
4. 有效线索转入 CRM。
5. 转入 CRM 后自动标记为新客户：`isNewCustomer = true`。
6. 转入 CRM 当天写入 `newCustomerDate`。

### 客户池复盘流转

1. `customer-review.html` 用于逐个销售复盘客户池。
2. 客户池复盘不是 CRM 的替代品。
3. 复盘后，应将需要持续跟进或留档复盘的客户明细录入 `crm.html`。

### 日报流转

1. `report.html` 读取 CRM、客户池复盘、抖音线索和 AI 研判。
2. 生成日报草稿。
3. 用户必须人工检查、编辑后再复制或保存。

## 5. 新客户与客户阶段使用规则

- 新客户不是客户阶段。
- `isNewCustomer` 表示是否新客户。
- `newCustomerDate` 表示新增日期。
- 客户阶段表示推进状态。
- 新客户可以同时是已量房、已报价或已签单。
- 今日新增客户按 `newCustomerDate` 统计。
- 客户阶段为“新线索”只表示当前推进状态，不等于新客户统计。
