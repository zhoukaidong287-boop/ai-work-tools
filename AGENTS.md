# AGENTS.md — ai-work-tools 项目修改规则

## 1. 项目定位

`ai-work-tools` 是一个个人主动营销作战工具库，不是普通展示网站。

核心目标：

- 客户池复盘
- CRM 客户跟进
- 抖音线索清洗
- AI 每日研判
- 日报/周报/阶段汇报输出
- 信息流素材实验室
- 投流测试与预算模拟
- 数据安全备份

项目第一原则：

真实可用 > 视觉高级 > 新功能数量

禁止为了视觉效果破坏业务逻辑。禁止为了重构破坏已有数据。禁止为了炫酷动效增加假按钮或半成功能。

## 2. 页面职责

`index.html`：
首页 / 工具入口 / Growth Command 平台概览 / 数据安全入口。

`crm.html`：
客户作战台 / CRM 主台账 / 客户跟进 / 死单管理 / 导入导出 / 数据备份。

`douyin-leads.html`：
抖音线索池 / 线索清洗 / 有效性判断 / 分配销售 / 转入 CRM。

`customer-review.html`：
销售客户池复盘 / 客户池草稿 / 销售维度汇总 / 跳转 CRM 录入。

`city-analysis.html`：
AI 每日研判台 / 读取系统数据 / 保存每日研判 / 活动政策资料库。原则上只读 CRM 主数据，不作为 CRM 主数据管理入口。

`report.html`：
今日日报生成器 / 读取客户池、CRM、线索和 AI 研判 / 生成可编辑日报 / 保存日报历史。唯一主任务：一键生成今日日报 → 检查编辑 → 复制/保存。

`material-lab.html`：
信息流素材实验室 / 素材矩阵 / AI 调令 / 素材历史。

`ad-test.html`：
投流测试与预算模拟 / 投流记录 / 预算判断。

禁止一个页面随意承担另一个页面的主职责。如果需要跨页读取数据，只能读取，不要复制另一页的完整管理逻辑。

## 3. 数据安全红线

- 任何修改不得清空 `localStorage`。
- 任何修改不得自动覆盖 CRM 主数据。
- 任何修改不得删除客户数据、日报历史、AI 研判历史、抖音线索、客户池历史。
- 任何导入、覆盖、清空动作必须先备份。
- 任何高危操作必须二次确认。
- 覆盖导入必须输入确认词。
- 清空 CRM 必须输入确认词：`清空CRM`。
- 覆盖导入 CRM 必须输入确认词：`覆盖导入CRM`。
- 全量覆盖导入必须输入确认词：`覆盖导入全部数据`。
- Codex 不得自动执行清空、覆盖、删除全部数据等高危动作。
- Codex 不得为了测试清空用户数据。

## 4. localStorage key 保护

核心 key：

- `decorationStoreCrmCustomers`：CRM 主数据 key，最高保护级别，禁止改名，禁止无备份覆盖。
- `customers`：旧 CRM 兼容 key，只读兼容，不建议继续写入。
- `douyinLeads`：抖音线索池数据。
- `customerReviewDraft`：客户池复盘草稿。
- `customerReviewRecords`：客户池复盘历史。
- `dailyAnalysisHistory`：每日 AI 研判历史，必须按日期读取。
- `pointReportRecords`：日报/周报/阶段汇报混合历史，必须按 `type` 过滤。
- `pointReportBaseInfo`：日报固定资料。
- `materialLabHistory`：信息流素材历史。
- `pointAdTestRecords`：投流测试记录。
- `pointProjectInfo`：项目基础资料，多页共用。

禁止修改现有 key 名称。新增 key 必须先写入 `DATA_SCHEMA.md`。任何读写 key 前必须确认是否已有旧兼容逻辑。

## 5. 修改范围控制

- 每次任务必须明确 allowed files。
- Codex 只能修改任务允许范围内的文件。
- 如果发现需要修改额外文件，必须先说明原因，不能自行扩大范围。
- 每次输出必须列出 changed files。
- 如果出现无关 changed files，必须回退无关修改。

禁止：

- 顺手改其他页面
- 顺手美化全站
- 顺手重构公共 CSS
- 顺手格式化业务文件
- 顺手改 localStorage 结构
- 顺手 Commit/Push

## 6. UI 修改规则

- UI 高级感必须服务于使用效率。
- 不能只追求炫酷。
- 不能做假科技线条、过度发光、黑金土豪风、币圈风、霓虹大屏风。
- Growth Command 视觉系统以冷灰、深色、克制、高对比为主。
- 按钮必须语义清楚。
- 输入区必须可读。
- 毛玻璃不能影响文字对比。
- 禁止白底浅字。
- 禁止深底黑字。
- 禁止按钮文字溢出。
- 移动端 390px 必须可用。
- 动效必须克制，不影响输入、复制和保存。

页面主次原则：

- 首页负责高级展示。
- 功能页负责高效操作。
- 日报页以日报正文为主角。
- CRM 页以客户列表和跟进动作为主角。
- AI 研判页以研判生成和保存为主角。

## 7. 业务逻辑修改规则

- 没有明确要求，不得修改业务逻辑。
- 没有明确要求，不得修改统计口径。
- 没有明确要求，不得修改客户阶段、死单、新客户、有效客户判断。
- 没有明确要求，不得修改抖音线索转 CRM 逻辑。
- 没有明确要求，不得修改日报生成逻辑。
- 没有明确要求，不得修改 AI 研判保存逻辑。

如果必须修改业务逻辑，必须先说明：

- 当前逻辑
- 问题
- 新逻辑
- 影响页面
- 是否影响旧数据
- 是否需要兼容旧数据
- 测试方式

## 8. 假按钮禁止规则

- 禁止出现看起来能点但实际不能点的按钮。
- 禁止 `href="#"` 无实际逻辑。
- 禁止 `onclick` 空函数。
- 禁止 tab 按钮不可切换。
- 禁止按钮点击后无反馈。
- 禁止复制按钮无复制结果提示。
- 禁止保存按钮无保存结果提示。

如果只是状态标签，视觉上不能像按钮。如果是按钮，必须有真实功能或明确提示。

## 9. 日期和业务日期规则

- 不能所有功能都死读系统当天。
- 日报、AI 研判、客户新增、跟进、逾期等都涉及日期。
- `report.html` 必须使用日报日期选择器。
- AI 研判必须按选择日期读取 `dailyAnalysisHistory`。
- 凌晨生成昨日日报时，必须允许用户选择昨日日期。
- `newCustomerDate` 必须保持稳定，不可随意重写。
- 历史保存时间可以记录真实操作时间，但业务统计必须按业务日期。

后续建议建立统一 date helper，但在没有专项任务前不得随意抽取。

## 10. Commit / Push 规则

- Codex 默认不得 Commit。
- Codex 默认不得 Push。
- 除非用户明确要求，否则只修改本地文件并输出验收报告。
- 每次完成后必须说明：
  - 当前 changed files
  - 是否可以 Commit
  - 是否建议继续修改代码
  - 是否发现风险

用户确认后再 Commit + Push。

## 11. 自检验收规则

每次任务完成后，Codex 必须自检：

1. changed files 是否在允许范围内。
2. 是否修改 localStorage key。
3. 是否清空数据。
4. 是否发现严重 JS 报错。
5. 是否影响核心页面打开。
6. 是否影响 CRM 主数据。
7. 是否影响日报/AI 研判读取。
8. 移动端 390px 是否可用。
9. 是否存在白底浅字/深底黑字。
10. 是否存在假按钮。

如果自检失败，先自行修复，再输出报告。

## 12. 代码屎山治理顺序

当前治理顺序：

- 第 1 刀：P0 数据安全加固，已完成。
- 第 2 刀：新增 AGENTS.md 项目约束，当前任务。
- 第 3 刀：`douyin-leads.html` 格式化，不改逻辑。
- 第 4 刀：统一日期 helper，解决跨日/业务日期问题。
- 第 5 刀：统一客户统计口径：新客户、死单、有效客户、阶段、动作。
- 第 6 刀：CRM 漏斗逻辑修正。
- 第 7 刀：AI 研判复制效率优化。
- 第 8 刀：CRM 编辑弹窗可读性修复。
- 第 9 刀：`shared-ui.css` 分层治理。
- 第 10 刀：`city-analysis` 职责拆分。
- 第 11 刀：`crm.html` 分区治理。

禁止跳过 P0/P1 直接做大规模 UI 或新功能。

## 13. Computer Use 使用原则

Computer Use 适合做验收，不适合替代产品设计。

Computer Use 应用于：

- 打开页面
- 点击按钮
- 检查是否显示结果
- 复制/保存/导出测试
- 移动端截图
- 页面交互验收

Computer Use 不应用于：

- 未经规则直接改业务逻辑
- 未经确认自动上传
- 代替数据安全判断

## 14. Skill / 指南使用原则

- 通用 Skill 可以参考，但项目规则优先。
- UI Skill 不能凌驾于数据安全和业务流程之上。
- 任何 Skill 输出都必须遵守 `AGENTS.md`、`DATA_SCHEMA.md`、`SYSTEM_RULES.md`、`PAGE_MAP.md`、`WORKFLOW.md`。
- 高级感不能牺牲真实可用性。

## 15. 每次输出格式

每次 Codex 完成任务后，输出：

1. 本次任务目标：
2. 当前 changed files：
3. 是否修改业务逻辑：
4. 是否修改 localStorage key：
5. 是否清空任何数据：
6. 是否发现严重 JS 报错：
7. 是否完成自检：
8. 是否建议继续修改代码：
9. 是否可以 Commit：
