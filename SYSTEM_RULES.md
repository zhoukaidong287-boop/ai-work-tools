# SYSTEM_RULES

本文档是 `ai-work-tools` 的项目规则。后续修改代码前，应先阅读本文件。

## AGENTS.md 优先级

- `AGENTS.md` 是 Codex 执行修改任务的入口规则文件。
- 任何代码修改前必须先读取 `AGENTS.md`。
- 如果 `AGENTS.md` 与临时任务冲突，以更严格的数据安全规则为准。
- 涉及 `localStorage`、CRM 主数据、导入导出、删除、覆盖、清空时，必须优先遵守 `AGENTS.md` 数据安全红线。

## 1. 数据安全规则

- `localStorage` 是当前系统主要数据源。
- 任何修改不得清空 `localStorage`。
- 任何修改不得删除客户数据。
- 任何修改不得删除跟进记录。
- 任何修改不得删除客户池复盘数据。
- 任何修改不得删除抖音线索池数据。
- 任何修改不得删除日报/周报历史。
- 任何修改不得删除 AI 研判历史。
- 新增字段必须兼容旧数据。
- 旧数据缺字段时不能报错。
- 删除跟进记录不等于删除客户。
- 删除跟进记录不应自动回退客户阶段。

### 数据安全红线

- 不得直接清空 `localStorage` 或绕过页面保护执行清空操作。
- CRM 导入、覆盖导入、清空前必须先自动备份当前 CRM 主数据。
- 不得修改 CRM 主数据 key：`decorationStoreCrmCustomers`。
- `customers` 只作为旧数据读取兼容，不得作为新主写入 key。
- `city-analysis.html` 只能只读 CRM 数据用于 AI 研判，不作为 CRM 主数据管理入口。
- 所有危险操作必须有明确确认提示。
- 覆盖导入必须要求用户输入确认词，不能只靠一次确认弹窗。
- 清空 CRM 必须要求用户输入确认词 `清空CRM`。
- 覆盖导入 CRM 必须要求用户输入确认词 `覆盖导入CRM`。
- 覆盖导入全部数据必须要求用户输入确认词 `覆盖导入全部数据`。
- Codex 不得自动执行清空、覆盖导入、删除正式数据等危险操作。
- 未经用户明确要求，Codex 不得 Commit / Push。

## 2. 修改范围规则

- 每次修改必须明确修改哪些文件。
- 只改用户要求的页面。
- 不要为了修一个小 bug 重构多个页面。
- 不要顺手改稳定功能。
- 不要随意修改页面入口。
- 不要随意更改 `localStorage` key。
- 不要随意更改核心字段名。
- 修改任何页面前，先确认该页面职责，不要把不属于该页面的功能硬塞进去。

## 3. Commit / Push 规则

- Codex 默认不要 Commit。
- Codex 默认不要 Push。
- 完成修改和测试后，只输出 changed files 和是否可以 Commit。
- 由用户决定是否 Commit / Push。

## 4. 测试规则

每次修改后必须检查：

- 页面能否打开。
- 浏览器控制台是否有严重 JS 报错。
- 旧数据是否仍能读取。
- 新功能是否能保存。
- 刷新页面后数据是否还在。
- 是否影响其他核心页面。
- 如果修改导入/导出，必须检查旧数据缺字段时不报错。

## 5. 页面业务边界

- CRM 是正式客户主台账。
- 抖音线索池是前置线索清洗池，不等于正式 CRM。
- 客户池复盘是销售维度复盘，不等于 CRM。
- `report.html` 负责读取数据并生成日报，不负责修改客户数据。
- `city-analysis.html` 负责 AI 研判和调令，不直接修改 CRM 客户。
- `material-lab.html` 负责素材矩阵和 AI 调令，不记录客户主数据。
- `ad-test.html` 负责投流测试和预算模拟。

## 6. 关键业务逻辑

- 新客户/新线索不是客户阶段。
- 是否新客户用 `isNewCustomer` 判断。
- 新增日期用 `newCustomerDate` 判断。
- 客户阶段表示推进状态，例如：已联系、已进店、已量房、已报价、已签单、死单。
- 一个客户可以同时是“新客户 + 已量房”。
- 一个客户可以同时是“新客户 + 已报价”。
- 一个客户可以同时是“新客户 + 已签单”。
- 新客户统计不能只看 `customerStage === "新线索"` 或 `status === "新线索"`。
- 今日新增客户应该按 `newCustomerDate` 统计。
- 死单客户不进入有效客户和今日待跟进。
- 跟进记录可以编辑。
- 跟进记录可以删除。
- 编辑跟进记录不应该重复新增记录。
- 删除跟进记录不删除客户。
- 删除跟进记录不应自动回退客户阶段。
