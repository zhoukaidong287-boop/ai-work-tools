# SYSTEM_RULES

本文档是 `ai-work-tools` 的项目规则。后续修改代码前，应先阅读本文件。

## 1. 数据安全规则

- `localStorage` 是当前系统主要数据源。
- 任何修改不得清空 `localStorage`。
- 任何修改不得删除客户数据。
- 任何修改不得删除跟进记录。
- 任何修改不得删除客户池复盘数据。
- 任何修改不得删除抖音线索池数据。
- 任何修改不得删除日报/周报历史。
- 任何修改不得删除 AI 研判历史。
- 新增字段必须兼容旧数据。
- 旧数据缺字段时不能报错。
- 删除跟进记录不等于删除客户。
- 删除跟进记录不应自动回退客户阶段。

## 2. 修改范围规则

- 每次修改必须明确修改哪些文件。
- 只改用户要求的页面。
- 不要为了修一个小 bug 重构多个页面。
- 不要顺手改稳定功能。
- 不要随意修改页面入口。
- 不要随意更改 `localStorage` key。
- 不要随意更改核心字段名。
- 修改任何页面前，先确认该页面职责，不要把不属于该页面的功能硬塞进去。

## 3. Commit / Push 规则

- Codex 默认不要 Commit。
- Codex 默认不要 Push。
- 完成修改和测试后，只输出 changed files 和是否可以 Commit。
- 由用户决定是否 Commit / Push。

## 4. 测试规则

每次修改后必须检查：

- 页面能否打开。
- 浏览器控制台是否有严重 JS 报错。
- 旧数据是否仍能读取。
- 新功能是否能保存。
- 刷新页面后数据是否还在。
- 是否影响其他核心页面。
- 如果修改导入/导出，必须检查旧数据缺字段时不报错。

## 5. 页面业务边界

- CRM 是正式客户主台账。
- 抖音线索池是前置线索清洗池，不等于正式 CRM。
- 客户池复盘是销售维度复盘，不等于 CRM。
- `report.html` 负责读取数据并生成日报，不负责修改客户数据。
- `city-analysis.html` 负责 AI 研判和调令，不直接修改 CRM 客户。
- `material-lab.html` 负责素材矩阵和 AI 调令，不记录客户主数据。
- `ad-test.html` 负责投流测试和预算模拟。

## 6. 关键业务逻辑

- 新客户/新线索不是客户阶段。
- 是否新客户用 `isNewCustomer` 判断。
- 新增日期用 `newCustomerDate` 判断。
- 客户阶段表示推进状态，例如：已联系、已进店、已量房、已报价、已签单、死单。
- 一个客户可以同时是“新客户 + 已量房”。
- 一个客户可以同时是“新客户 + 已报价”。
- 一个客户可以同时是“新客户 + 已签单”。
- 新客户统计不能只看 `customerStage === "新线索"` 或 `status === "新线索"`。
- 今日新增客户应该按 `newCustomerDate` 统计。
- 死单客户不进入有效客户和今日待跟进。
- 跟进记录可以编辑。
- 跟进记录可以删除。
- 编辑跟进记录不应该重复新增记录。
- 删除跟进记录不删除客户。
- 删除跟进记录不应自动回退客户阶段。
