# UI_DESIGN_SYSTEM

本文档记录 `ai-work-tools` 的平台化 UI 规范。后续 UI 修改应先阅读本文档，并遵守 `SYSTEM_RULES.md` 中的数据安全规则。

## 1. 平台视觉定位

平台名称建议：主动营销点将作战平台。

视觉关键词：

- 专业
- 高效
- 数据化
- 作战感
- 管理看板
- 现场可用
- 移动端友好

避免：

- 作业感
- 纯表单堆砌
- 大段说明堆叠
- 按钮杂乱
- 页面像临时测试工具

## 2. 色彩规范

建议使用：

- 深色主标题文字。
- 浅灰背景。
- 白色或浅色卡片。
- 蓝色作为主操作色。
- 绿色表示有效、完成、签单。
- 橙色表示待跟进、风险提醒。
- 红色表示死单、逾期、删除。
- 紫色或深蓝用于 AI 研判、智能模块。

不要使用过多颜色。标签和按钮应服务于业务状态，不做装饰性堆色。

## 3. 布局规范

每个页面尽量统一使用：

- 顶部平台标题区。
- 页面副标题说明。
- 关键数据看板区。
- 核心操作区。
- 历史记录或列表区。
- 使用提示或风险提醒区。

页面最大宽度建议在 `1180px` 到 `1280px` 之间。移动端保留清晰上下间距，不追求桌面端信息密度。

## 4. 组件规范

### 顶部标题

- 使用清晰业务名称，例如“CRM客户作战台”“抖音线索清洗台”。
- 副标题说明页面职责，不写冗长操作说明。
- 重要返回按钮放在顶部明显位置。

### 模块卡片

- 使用白色或浅色背景。
- 圆角保持一致。
- 通过标题、说明和间距区分模块。
- 不嵌套过多卡片。

### 数据指标卡

- 包含标题、数字、简短说明。
- 核心数据优先放在页面上方。
- 缺少数据时显示 0 或“暂无数据”，不能报错。

### 按钮

- 主按钮：蓝色或蓝紫渐变。
- 次按钮：浅色底、深色文字或描边。
- 有效/完成：绿色。
- 风险/提醒：橙色。
- 删除/死单：柔和红色。
- 按钮文字必须清晰可读。

### 标签和状态徽章

- A/B/C、死单、新客户、今日跟进、逾期等状态必须明显区分。
- 标签不能过大，避免挤压列表内容。

### 表单输入框

- 字段按业务分组。
- 移动端单列显示。
- 必填字段要靠前。
- 多选按钮要可换行，不横向溢出。

### 表格

- 桌面端允许横向滚动。
- 操作列必须可见或可通过横向滚动访问。
- 手机端优先保证按钮可点击。

### 空状态

- 空列表显示明确提示，例如“暂无抖音线索”“暂无历史记录”。
- 空状态不应让用户误以为页面加载失败。

## 5. 移动端规则

- 手机端优先可用。
- 按钮不能太小。
- 表格允许横向滚动。
- 核心按钮放在明显位置。
- 不要让用户左右乱滑找按钮。
- 表单字段要分组。
- 卡片间距要清楚。
- 页面顶部标题不要过大导致首屏浪费。

## 6. 页面升级原则

- 只改 UI 不改业务逻辑。
- 不改 `localStorage` key。
- 不改数据结构。
- 不删除旧功能。
- 不破坏旧数据。
- 每次升级一个页面也必须保留原功能。
- 页面改完必须测试保存、读取、刷新是否正常。
- 如果 UI 修改引发 JS 报错，必须优先修复。

## 7. 页面定位

- 首页是平台驾驶舱。
- CRM 是客户作战台。
- 抖音线索池是前端线索清洗台。
- 客户池复盘是销售客户盘点台。
- AI 研判是作战参谋台。
- 日报是复盘输出台。
- 素材矩阵是内容作战实验室。
- 投流测试是预算测试台。

## 8. 首页高端暗黑交互驾驶舱规范

首页采用黑金克制的高端业务驾驶舱风格，是平台门面，不是工具目录。首页应体现“主动营销点将作战平台 · Command Center”的整体感，优先传达作战闭环、真实数据驾驶舱和模块入口，而不是堆叠普通后台卡片。

首页必须包含：

- Hero 首屏。
- 今日作战概览。
- 核心模块入口。
- 每日作战流程。
- 风险提醒。
- 数据安全中心。

首页视觉允许使用：

- CSS 光效。
- 玻璃拟态。
- 细线边框。
- 编号模块。
- 暗黑背景。
- 橙金重点光效。
- 轻量 hover、跳转、滚动交互。

首页数据规则：

- 首页只能只读 `localStorage`。
- 首页不得为了统计写入业务数据。
- 首页不得修改 `localStorage` key。
- 首页不得清空客户、线索、日报、AI 研判、客户池复盘或素材历史。
- 读取失败时显示 0 或通用提醒，不允许报错阻断页面。

首页交互规则：

- 交互以顶部导航、模块跳转、查看今日数据滚动、风险提醒跳转、卡片 hover 为主。
- 不允许引入复杂依赖。
- 不允许使用 React/Vue/后端。
- 不允许为了高级感牺牲录入效率。
- 功能页后续可以逐步迁移为深色高级工作台，但每次迁移必须遵守页面业务边界和数据安全规则。

### 首页设计纠偏规则

- 高级感不是堆英文和科技装饰。
- 首页视觉元素必须有明确业务含义。
- Hero 右侧区域优先展示真实作战数据，而不是抽象科技浮层。
- 少用无意义英文，不用英文替代业务解释。
- 不使用 `DATA ROUTING`、`COMMAND STATUS`、`LIVE` 这类看不懂的装饰词。
- 首页以黑金克制风格为主，避免蓝橙科技模板感。
- 减少复杂光圈、强网格、多层科技线条和模板化浮层。
- 首页要像老板或主动营销老师会打开看的高端业务驾驶舱，不像科技模板。

### 极简品牌首页原则

- 首页不是数据大屏。
- 首页不是后台面板集合。
- 首页要像高端品牌官网，先表达平台主张，再给出少量高频入口。
- 首屏只保留核心主张、主视觉和少量关键数据。
- 高级感来自克制、留白、主视觉和清晰层级，不是面板数量。
- 避免 AI 生成图感。
- 避免币圈模板感。
- 首页信息密度必须低于功能页。
- 核心能力入口优先保留 3 到 4 个最高频模块，其他工具用轻量文字链接承接。
- 首页数据只展示能辅助当天决策的少量关键指标，详细漏斗和历史数据留在功能页。

### 首页主视觉规则

- 高级首页必须有清晰主视觉锚点。
- 右侧主视觉不能使用无意义占位线条。
- 产品预览插图要体现真实业务模块，例如客户列表、客户推进漏斗、AI 研判、日报输出。
- 首页首屏需要形成背景层、中景层、前景层：背景负责氛围，中景负责产品预览，前景负责标题、按钮和少量关键数据。
- 数据指标要轻量化，避免做成后台统计卡。
- 主视觉要克制，不要变成炫酷数据大屏。
- 可以用 CSS/HTML 模拟产品界面轮廓，但必须让用户看得出业务含义。

### 首页真实主视觉资产规则

- 高端首页必须优先使用项目内主视觉资产，纯 CSS 光影不能替代真实主视觉。
- 首页主视觉图片建议使用 `assets/hero-command-center.png` 或同类本地资产。
- 主视觉图片必须无文字、无人物、无 Logo。
- 主视觉应服务于业务氛围，例如暗黑黑金作战指挥中心、城市夜景、数据光束、控制台氛围。
- 首页不做数据大屏，只保留少量关键指标。
- 首页核心是品牌感、主视觉、清晰入口。
- 数据卡必须克制，不能像后台统计卡。
- 不允许用大量灰色占位卡片伪装高级。
- 右侧视觉必须有业务含义，少量浮层只用于表达 CRM 推进、AI 研判、日报输出等核心模块。
- 首页只读 localStorage，不写入业务数据。

### 首页真实主视觉接入规范

- 首页必须使用真实主视觉图作为首屏视觉锚点，而不是仅依赖 CSS 卡片、渐变和光效。
- `assets/hero-command-center.png` 应作为 Hero 首屏背景铺满，使用 `background-size: cover` 与暗色遮罩保证文字清晰。
- 数据浮层应压在真实主视觉图上，作为业务辅助信息，而不是替代主视觉本身。
- 右侧区域不得再使用灰色占位面板、占位线条或无业务含义的假界面。
- 首页不做数据大屏，只展示少量关键数据；详细漏斗、客户池和历史数据留给功能页。
- 高级感来自真实视觉资产、留白、层级、克制的黑金色彩和清晰业务入口。
- 首页图片必须无文字、无人物、无 Logo；图片缺失时必须有暗黑渐变兜底，不得阻断页面。
- 首页只能只读 localStorage，用于展示统计，不得写入、清空或迁移业务数据。

## 当前最终 UI 方向：A+B

后续 UI 升级必须优先遵守 `UI_SKILL_GUIDE.md`。如本文档中的旧规则与 `UI_SKILL_GUIDE.md` 冲突，以 `UI_SKILL_GUIDE.md` 的最新方向为准。

- A = 冷灰奢华品牌官网风。
- B = 高级科技产品平台感。
- A 为主，B 为辅。
- 不再使用黑金作为主方向，允许少量香槟白或银色高光，但禁止黑金土豪风。
- 禁止币圈风、假科技风、网吧科技风、学生作业风、灰色占位稿和卡片堆砌。
- 首页必须有真实主视觉，首页不是数据大屏，也不是后台面板集合。
- 首页首屏只保留品牌主张、真实主视觉、少量关键数据和核心行动按钮。
- 功能页是高级工作台，必须保证结构清晰、数据可读、表单好填、操作高效。
- 数据和功能服务于业务，不抢主视觉，不为了装饰牺牲现场使用效率。
- 所有 UI 改动不得修改 localStorage key，不得清空 localStorage，不得删除客户、线索、日报、AI 研判、客户池复盘或素材历史。
- 除非用户明确要求，UI 任务不修改业务 JS、不修改 CRM 数据结构、不修改抖音线索转 CRM、新客户/客户阶段、死单、今日待跟进或逾期未跟进逻辑。
- 默认不要 Commit，默认不要 Push；完成后必须输出 changed files 和是否可以 Commit。

## 首页 A+B 高级视觉规范

- A = 冷灰奢华品牌官网风。
- B = 高级科技产品平台感。
- A 为主，B 为辅。
- 首页不是数据大屏，不是后台首页，不是工具目录。
- 首页首屏必须有真实主视觉资产，优先使用 `assets/hero-command-center.png` 或 `assets/hero-command-center.webp`。
- 首屏只保留品牌主张、少量说明文字、1-2 个核心按钮和最多 3 个关键数据。
- 首屏关键数据只允许展示：今日新增、今日待跟进、逾期未跟进。
- 第二屏只展示 4 个核心能力：CRM客户作战台、抖音线索清洗台、AI每日研判台、日报复盘输出台。
- 销售客户池复盘、信息流素材实验室、投流测试与预算模拟只能作为“更多工具”轻量入口。
- 第三屏可以展示产品能力，但必须克制，建议只展示 CRM、AI 研判、日报输出。
- 不使用黑金土豪风，不使用币圈模板风，不使用假科技光效，不使用灰色占位面板。
- 不堆卡片，不堆流程，不堆 6-8 个数据指标，不把首页做成大屏驾驶舱。
- 首页主色应转向冷灰、炭黑、暗蓝灰、冷白、银灰和低透明边框；避免大面积橙金和土豪金渐变。
- 首页只读 localStorage，不写入业务数据，不修改 localStorage key，不清空或迁移任何客户、线索、日报、AI 研判、素材或客户池复盘数据。

## 图片元素使用规则

- 首页必须由真实主视觉图驱动，不能只靠深色背景、卡片和 CSS 光效撑起视觉。
- 主视觉图片必须明显可见，遮罩只用于保证文字可读，不得把图片压到几乎看不见。
- 首页图片不是装饰，而是品牌视觉锚点；文字、按钮和数据浮层都应服务于主视觉。
- 卡片和数据浮层不能抢主视觉，透明度、边框和阴影必须克制。
- 右侧数据浮层只能作为轻量业务提示，不应成为厚重后台面板。
- 第三屏产品展示必须有产品图感，不能只是占位线、灰色横条或未加载完成的线框稿。
- 如果没有真实截图，可以用 CSS/HTML 模拟，但必须像真实产品界面：有导航、列表、标签、状态、详情或数据关系。
- 禁止灰色占位稿，禁止纯卡片堆砌，禁止用无意义线条伪装高级。

## 首页真实照片级主视觉规则

- 首页主视觉必须优先使用真实照片级视觉资产，例如高端空间摄影、写实建筑空间、落地窗城市夜景、冷灰商务空间或高质量写实渲染。
- 不允许使用科技线条、几何线框、假 UI 面板、灰色占位卡片、数据大屏或暗色渐变来冒充主视觉图片。
- 主视觉图应无人物、无文字、无 Logo、无 UI 面板、无数据屏、无线条科技图，避免黑金土豪风、币圈风和假科技大屏感。
- 图片必须在首屏明显可见；遮罩、数据面板和指标条必须服从图片，不得抢画面。
- 如果没有合格图片，应明确提示需要用户上传真实主视觉图片，不要强行用 CSS 线条或卡片假画。

## 首页品牌与动效规则

- 当前首页品牌名只使用英文 `Growth Command`。
- 不再使用中文品牌名“增长指挥舱”，也不再使用“主动营销点将作战平台”作为首页品牌。
- 品牌名不得中英混排；不要写成 `Growth Command 增长指挥舱`。
- 中文只用于功能说明、按钮、导航和业务描述。
- 首页允许克制字体动效，动效以 `opacity` 和 `transform` 为主。
- Hero 小标题、主标题、副标题、说明文案、按钮和关键数据可以使用错峰淡入上浮。
- 数字递增动效只影响 DOM 显示，不写入 localStorage，不修改客户数据。
- 必须支持 `prefers-reduced-motion`，用户开启减少动态效果时应直接显示最终内容。
- 当前图片质感如不够真实，后续应替换真实摄影感主视觉图，不得用 CSS 线条、灰色面板或假 UI 伪造。

## 首页交互方向：C 混合型

- C = 混合型：第一屏做高级氛围交互，第二屏做平台能力模块交互，第三屏做克制产品展示交互。
- 第一屏背景可以根据鼠标位置轻微响应，例如轻微视差、冷灰光晕和玻璃反光变化，但必须克制。
- 不做真正天气切换，不做游戏化效果，不做强烈闪烁、粒子动画、three.js 或大型 3D。
- 第二屏负责平台能力模块交互：滚动进入淡入上浮，hover 轻微上浮和边框提亮，点击整块跳转。
- 第三屏负责克制产品展示交互：产品卡可轻微上浮和提亮，但禁止低保真线框图、灰色占位稿和未加载完成感。
- 所有动效必须支持 `prefers-reduced-motion`，移动端不强制启用鼠标背景响应。
- 所有交互只作用于视觉表现，不写入业务数据，不修改 localStorage key。
- 动效服务于高级感、空间感和可读性，不抢业务内容。

## 首页环境交互规则

- 鼠标交互必须是环境级微响应，不是手电筒效果。
- 禁止局部强光斑跟随鼠标，禁止明显圆形 spotlight，禁止鬼片式局部照明。
- 背景响应幅度必须极轻：只允许细微空间位移、整体亮度微调和整体对比度微调。
- 鼠标位移只用于制造细微空间感，不得制造强烈暗角变化或局部扫光。
- 背景亮度建议控制在 `brightness(1)` 到 `brightness(1.035)`，对比度建议控制在 `contrast(1)` 到 `contrast(1.03)`。
- 移动端关闭鼠标响应，只保留静态主视觉和基础进入动效。
- `prefers-reduced-motion` 开启时关闭背景动画和鼠标氛围响应。

## 首页首屏动效修正规则

- Hero 背景图应作为静态高级视觉层，不使用 mousemove 驱动背景位移。
- 不允许对大面积 Hero 背景图做动态 `filter`、`brightness`、`contrast`、`blur` 或频繁 `background-position` 更新。
- Hero 入场动效以慢速 `opacity + translateY` 为主，主标题建议 1.1s-1.3s，副标题建议 0.9s-1.1s，按钮建议 0.8s-1.0s。
- Hero 标题可对 1-3 个关键词增加 hover 微交互，但只允许轻微提亮和 1px-2px 上移，不逐字跳动、不抖动、不强发光。
- 数字递增建议 1.2s-1.6s，只影响显示，不反复触发，不写入数据。
- 移动端关闭非必要动效；`prefers-reduced-motion` 开启时关闭 Hero 入场、数字递增和 hover 位移动效。

## Growth Command 功能页工作台规则

- 首页是品牌官网风，功能页是深色高级工作台，不再使用大图 Hero 或数据大屏。
- 功能页统一使用 `Growth Command` 顶部导航，并提供：首页 / CRM / 线索 / 客户池 / AI研判 / 日报 / 素材 / 投流。
- 功能页视觉以冷灰、暗色、细边框、低透明面板为主，禁止黑金土豪风、币圈风、假科技线条和灰色占位稿。
- 功能页可以引用 `shared-ui.css` 作为公共样式层；该文件只能负责视觉、布局和交互，不得写业务逻辑。
- 页面标题使用英文工作台命名：`CRM Workspace`、`Leads Workspace`、`Customer Review`、`AI Insight`、`Daily Report`、`Material Lab`、`Ad Test`。
- 表单、列表、输出区、历史区必须保持可读，按钮层级清楚，移动端 390px 下不能横向溢出。
- UI 升级不得修改 `localStorage` key，不得改 CRM、抖音线索、客户池复盘、日报、AI研判、素材或投流的保存和读取逻辑。
- 功能页交互只保留轻量 hover、焦点边框和区块淡入；禁止鼠标背景跟随、粒子、three.js、复杂转场和强光效。

## 全站可读性与对比度规则

- UI 升级不能牺牲可读性，功能页首先是可录入、可查看、可复盘的工作台。
- 深色背景必须配浅色高对比文字，主文字建议接近 `#F4F6F8` 或 90% 以上白色透明度，副文字不应低于约 62% 白色透明度。
- 浅色背景必须配深色文字，主文字建议 `#111827`，副文字建议 `#475569`。
- 表格、筛选区、输入框、按钮、标签和历史记录必须优先保证现场可用，不能为了高级感降低辨识度。
- 禁止白底浅字、白底白字、深底黑字、低对比按钮、表头不可读和操作列不可读。
- A/B/C、死单、新客户、今日待跟进、逾期未跟进、已转CRM等状态标签必须在当前背景上清晰可读。
- 如果功能页采用深色工作台风格，表格和筛选区也应同步深色化；如果保留浅色区域，必须使用深色文字。
- 每次 UI 改动后必须逐页检查文字对比度，尤其检查 CRM 客户列表、筛选区、表头、操作按钮、弹窗和移动端。

## 全站按钮与标签语义颜色规则

- 按钮必须有操作优先级，主要动作、次要动作、正向动作、警告动作和危险动作必须视觉区分。
- 查看、复制类按钮使用冷灰或蓝灰；跟进、读取类按钮使用冷蓝；编辑、保存类按钮使用低饱和青绿色；删除、死单、无效类按钮使用红色危险语义。
- A/B/C 客户标签必须一眼能区分：A 类偏绿色，B 类偏冷蓝，C 类偏灰紫或低饱和紫色，死单偏红色。
- 客户阶段标签必须按业务含义区分颜色：新线索偏青色，已联系偏蓝灰，已进店偏蓝色，已量房偏青绿，已报价偏琥珀，已签单偏绿色，沉睡偏灰紫，死单偏红色。
- 全站标签颜色必须统一，不能同一个业务状态在不同页面显示成完全不同语义。
- 高级感不能牺牲识别效率，禁止所有标签都使用同一种灰色胶囊样式。
- 危险按钮不能和普通按钮看起来一样；删除、清空、标记死单必须有明确危险色和可读文字。
- 按钮和标签必须通过移动端可读性检查，390px 宽度下不能重叠、挤爆或误触。

## 历史记录与编辑器可读性规则

- 历史记录卡片必须与当前主题保持一致，不能出现白底浅字、白底白字或深底黑字。
- `textarea`、读取结果、摘要编辑器、AI 调令、日报正文和素材输出区必须高对比可读。
- 所有保存历史、AI研判历史、日报历史、素材历史、投流历史和客户池复盘历史都必须逐页检查。
- UI 升级不能只检查表格，还必须检查历史卡片、空状态、编辑器、输出区、读取结果和动态渲染卡片。
- 浅色容器如果确需保留，必须使用明确的深色文字；Growth Command 功能页优先使用深色工作台容器。
- 禁止浅色容器内使用浅色文字；发现残留浅底容器时，应优先通过公共样式兜底修复。

## Daily Report 页面可读性规则

- 日报页所有摘要、编辑器、读取结果、历史记录必须高对比可读。
- `textarea`、客户池与CRM数据摘要、今日AI研判摘要、日报正文编辑器不能出现白底浅字。
- 本周日报读取结果、日报历史、周报历史和AI研判引用区域必须逐条检查。
- 读取、生成、保存、复制、清空、删除按钮必须按语义颜色显示，并保证文字清楚。
- 日报页是工作台，不是白色文档卡片堆叠；优先使用深色工作台容器。
- UI 升级不得破坏日报读取、生成、编辑、保存、复制、下载逻辑，不得修改 `localStorage` key。
